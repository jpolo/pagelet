/*jslint nodejs:true, browser:true, indent:2 */
/*global define*/
define("pagelet/spec/all",
[
  "pagelet/spec/pagelet_spec"
],
function () {
  if (typeof console !== 'undefined') {
    console.log("Suite loaded");
  }
});
